<%= @event.title %>

<%= @event.creator.name %>

<% if @event.attendees.any? %>
	
	<ul>
		<% @event.attendees.each do |attendee| %>
			<li> <%= attendee %></li>
		<% end %>
	</ul>
<% else %>
	
	<p>Nobody invited</p>
	
<% end %>